package com.example.demowallet

import androidx.compose.runtime.getValue
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.setValue
import androidx.compose.ui.graphics.Color

/** Brand + surface colours shared across the app (theme can mutate brand colours). */
var RenPurple by mutableStateOf(Color(0xFF00B14F))
var RenViolet by mutableStateOf(Color(0xFF00C853))
var RenBlue by mutableStateOf(Color(0xFF00B14F))

val RenDark = Color(0xFF0A0F0D)
val RenCard = Color(0xFF1C2621)
val RenCard2 = Color(0xFF24302A)
val RenText = Color(0xFFF5FBF7)
val RenMuted = Color(0xFF9AB5A5)
val RenGreen = Color(0xFF00C853)
val RenRed = Color(0xFFFF5252)
val RenOrange = Color(0xFFFFB300)
