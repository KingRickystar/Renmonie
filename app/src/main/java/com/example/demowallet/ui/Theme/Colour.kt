package com.example.demowallet.ui.theme

import androidx.compose.ui.graphics.Color

// ============================================================
// RenMonie v1.3 — OPay-inspired Premium Fintech Colors
// ============================================================

// Primary brand (OPay-style emerald green)
val RenPrimary = Color(0xFF00B14F)
val RenPrimaryDark = Color(0xFF009140)
val RenPrimaryLight = Color(0xFF33C470)
val RenPrimaryContainer = Color(0xFF0A2E1A)

// Secondary / accent
val RenSecondary = Color(0xFF00C853)
val RenAccent = Color(0xFFFFD54F)
val RenAccentDark = Color(0xFFF9A825)

// Backgrounds (deep premium dark)
val RenBackground = Color(0xFF0A0F0D)
val RenSurface = Color(0xFF121A16)
val RenSurfaceVariant = Color(0xFF1A2420)
val RenCard = Color(0xFF1C2621)
val RenCardElevated = Color(0xFF24302A)

// Text
val RenTextPrimary = Color(0xFFF5FBF7)
val RenTextSecondary = Color(0xFF9AB5A5)
val RenTextTertiary = Color(0xFF6B8578)
val RenTextOnPrimary = Color(0xFFFFFFFF)

// Status
val RenSuccess = Color(0xFF00C853)
val RenWarning = Color(0xFFFFB300)
val RenError = Color(0xFFFF5252)
val RenInfo = Color(0xFF40C4FF)

// Dividers & borders
val RenDivider = Color(0xFF2A3830)
val RenBorder = Color(0xFF334038)

// Overlay
val RenOverlay = Color(0x99000000)

// Legacy aliases for compatibility during upgrade
val RenMonieNavy = RenBackground
val RenMonieNavyCard = RenCard
val RenMonieBlue = RenPrimary
val RenMonieBrightBlue = RenPrimaryLight
val RenMonieCyan = Color(0xFF20C7D9)
val RenMonieBackground = RenBackground
val RenMonieSurface = RenSurface
val RenMonieSurfaceLight = RenSurfaceVariant
val RenMonieText = RenTextPrimary
val RenMonieTextSecondary = RenTextSecondary
val RenMonieTextDisabled = RenTextTertiary
val RenMonieSuccess = RenSuccess
val RenMonieWarning = RenWarning
val RenMonieError = RenError
val RenMonieWhite = Color(0xFFFFFFFF)
val RenMonieBlack = Color(0xFF000000)
val RenMonieDivider = RenDivider
val RenMonieCardHighlight = RenCardElevated
val RenMonieOverlay = RenOverlay
